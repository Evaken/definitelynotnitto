{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    // Node types are needed by the source-boundary test (fs).  DOM types are
    // deliberately absent: game-core must never touch the browser.
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
